topk: 51
exp_name: 'radio_378_val_knn'
folder: 4
emb_space: 'radio-378' # so far can be 'clip', 'openclip', 'siglip', 'dino_old'
lower_bound: 0  # Number of the image in the folder at which the calculation should start
upper_bound: 50000 # Number of sthe image in the folder at which the calculation should stop
nbrs_conf: 0.99  # Threshold for skipping neighbors with low confidence
is_normalized: 0  # Flag are embeddings have been normalized
mode: 'def'  # def, few-shot, kfold
split: 'val'  # Split where to search for neighbours
m_sample: -1
n_iterations: 125
seed: 0
kfold: 5