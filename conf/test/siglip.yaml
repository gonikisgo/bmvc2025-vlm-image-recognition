model: 'SigLIP'
exp_name: 'siglip_openai_0'
folder: 'siglip' # Folder name to save results
labels_option: 'mod'  # Class name strings to tokenize (VIT models only)
context: '0'
dataloader: 'val'  # 'val' for loading validation data, 'train' for loading train data
num_workers: 8
batch_size: 5
topk: 5
mode: 'base'  # Mode flag for saving
save: True
emb_space: siglip
